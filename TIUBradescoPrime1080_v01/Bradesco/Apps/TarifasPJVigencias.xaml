<UserControl x:Class="Bradesco.Apps.TarifasPJVigencias"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" Width="1080" Height="1440">

    <UserControl.Triggers>
        <EventTrigger RoutedEvent="UserControl.Loaded">
            <EventTrigger.Actions>
                <BeginStoryboard Storyboard="{StaticResource showItemsStoryboard}" />
                <BeginStoryboard Storyboard="{StaticResource showItemsMenuStoryboard}" />
            </EventTrigger.Actions>
        </EventTrigger>
    </UserControl.Triggers>

    <Canvas Name="canvasTarifa" ClipToBounds="True" >
        <Image Width="1080" Height="1440" Source="/Bradesco;component/Imagens/telas/tarifasPJVigencias.png" />
        <ContentControl Tag="0" Canvas.Left="32" Canvas.Top="15" Height="50" Width="1015" TouchDown="OpenTarifasPJ_old" MouseDown="OpenTarifasPJ_old">
            <TextBlock x:Name="vigPJOldText" Margin="85,10,0,10" HorizontalAlignment="Left" VerticalAlignment="Center" Foreground="#FF303030" FontFamily="NewJuneHeavy" FontSize="24" Width="927">Old</TextBlock>
        </ContentControl>
        <ContentControl Tag="1664" Canvas.Left="32" Canvas.Top="84" Height="50" Width="1015" TouchDown="OpenTarifasPJ" MouseDown="OpenTarifasPJ">
            <TextBlock x:Name="vigPJNewText" Margin="85,10,0,10" HorizontalAlignment="Left" VerticalAlignment="Center" Foreground="#FF303030" FontFamily="NewJuneHeavy" FontSize="24" Width="928">New</TextBlock>
        </ContentControl>
    </Canvas>

</UserControl>
